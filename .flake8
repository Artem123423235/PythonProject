# .flake8
[flake8]
# Максимальная длина строки (совместимо с Black в проекте)
max-line-length = 119

# Игнорируем правила, конфликтующие с Black
extend-ignore = E203, W503

# Папки/файлы, которые не проверяются (пути относительно корня проекта)
exclude = .git, __pycache__